{
  "name": "christmedical-monorepo",
  "private": true,
  "description": "Monorepo root — Next.js app lives in frontend/",
  "scripts": {
    "vercel-build": "cd frontend && npm ci && npm run build"
  }
}
